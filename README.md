# Strut Motors Landing Page

Single‑page, static landing page based on the Strut Motors deck.

## Local preview

- Open `index.html` directly, or run:
  - `python3 -m http.server 5173`
  - then visit `http://localhost:5173`

